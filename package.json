{
  "name": "blog-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix Backend",
    "frontend": "npm run dev --prefix cms-project",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
